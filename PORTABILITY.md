# Form Module

Have all components required to build a form.

## Portability

Run the following:

```sh
pnx shadcn@latest add field input input-group tooltip button command popover scroll-area combobox calendar radio-group select skeleton slider switch textarea "@wds/loading-swap" "@diceui/file-upload"
```
